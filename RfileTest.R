# Markov Language R File

# This is a test file just to make sure there are no
#   issues collaborating i.e. that we can all check out
#   a file, edit, add it, commit the changes, and push
#   the changes

a <- runif(n = 10)
b <- runif(n = 10)
plot(a, b)